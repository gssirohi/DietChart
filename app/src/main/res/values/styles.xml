<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppButtonStylePrimary" parent="Text.Light.Large">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textAllCaps">true</item>
        <!-- <item name="android:textStyle">bold</item> -->
        <item name="android:background">@drawable/bg_button_primary</item>
    </style>

    <style name="AppButtonStyleNegative" parent="Text.Light.Large">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textAllCaps">true</item>
        <!-- <item name="android:textStyle">bold</item> -->
        <item name="android:background">@drawable/bg_button_negative</item>
    </style>

    <style name="AppButtonStyleAccent" parent="Text.Light.Medium">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textAllCaps">true</item>
        <!-- <item name="android:textStyle">bold</item> -->
        <item name="android:background">@drawable/bg_button_accent</item>
    </style>

    <style name="Card.Title" parent="Text.Light.VeryLarge.Bold">
        <item name="android:textColor">@android:color/white</item>
    </style>

</resources>
